import React from "react";

const Home = ({ data }) => {
  return (
    <div id="home">
      <h2 className="text-white">SpaceX cargo planner</h2>
      <p className="text-white">Choose shipment from "Shipment list".</p>
    </div>
  );
};

export default Home;
